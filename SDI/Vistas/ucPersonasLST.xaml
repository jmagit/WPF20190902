<UserControl x:Class="SDI.Vistas.ucPersonasLST"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SDI.Vistas"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock FontSize="24" FontStyle="Italic" >Mantenimiento de <LineBreak/> <Bold>Personas</Bold></TextBlock>
        <DataGrid Grid.Row="1" />
        <StackPanel Grid.Column="1" Grid.RowSpan="2" >
            <Button Click="Ver_Click">Ver</Button>
            <Button>Añadir</Button>
            <Button>Modificar</Button>
            <Button>Borrar</Button>
            <Button Click="Salir_Click">Salir</Button>
        </StackPanel>

    </Grid>
</UserControl>
